# Yekan Bakh Pro (panel font)

Copied from `Yekan Bakh (Pro) @MafiaFont` webfonts + variable + family TTFs.

- Runtime loader uses `woff2/` via `src/lib/fonts/yekan-bakh.ts`
- Do not commit license PDFs here
