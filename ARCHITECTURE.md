میخوام یه سیستم کامل و جامع و بدون نقص داشته باشم که به صورت فازبندی شده بریم جلو.
۱. بک‌اند لاراول هست، رانت اند ری‌اکت و با کامپوننت‌های رسمی shadcn هست.
برای صفحه لاگین:
npx shadcn@latest add login-04

برای داشبورد از کامپوننت‌های رسمی ShadCN:
npx shadcn@latest add sidebar-07
نکته؛ حتما باید از نسخه رسمی shadcn استفاده بشه و ایمپورت (فچ) کنی و تحت هیچ شرایطی از روی خودت طراحی نکنی.

قوانین الزامی Cursor: `.cursor/rules/` (هسته، بک‌اند، shadcn، i18n، ماژول/تم استارتر، فونت).
———
۲. معماری به صورت دو زبانه هست و هرکاری صورت میگیره باید به زبان فارسی و انگلیسی انجام بشه.
توی زبان فارسی همه چیز راست چین، تاریخ‌ها شمسی، دیت‌پیکر شمسی و اعداد فارسی نمایش داده میشن.
توی زبان انگلیسی همه چیز چپ چین، تاریخ‌ها میلادی، دیت پیکر میلادی و اعداد انگلیسی نمایش داده میشن.

۳. دارک مود - لایت مود + اکسنت‌های رنگی باید پیاده بشه (نسخه رسمی shadcn)

۴. همه متن‌ها باید توی فایل ترجمه باشن - به صورت مرتب و دسته‌بندی شده - هیچ متنی هارد کد نباشه.

۵. میخوام یه سیستم ماژولار باشه که فرانت‌اند - بک‌اند توی یه کانتینر باشن و هر مشتری قابل مدیریت باشه.

۶. داشبورد باید هر قسمتش یه ماژول جداگانه باشه و امکان خاموش / روشن کردن رو داشته باشه.

۷. کل سیستم قراره برای کسب و کارهای مختلف UI/UX مختلفی داشته باشه.
برای مثال برای کافه منوها و...  با فروشگاه لوازم آرایشی فرق داره - باید اول کلیات آماده بشه و بعد برای هر کسب و کار شخصی‌‌سازی بشه.
———
توضیحات: قراره این داشبورد به صورت ماژولار باشه و به گیت مرکزی متصل باشه و همه ماژول‌ها رو بتونه از گیت دانلود و فعال / غیرفعال کنه.
هر ماژول ممکنه هزینه جداگانه برای کاربر داشته باشه و باید همه ماژول‌‌ها از CRM اصلی شرکت لایسنس براشون صادر بشه.
مثلا کسب و کار الف که کافه هست، لایسنس کافه براش صادر بشه - تمام ماژول‌های کافه براش قابل دانلود هستن - در صورتی که بخواد چیز دیگه‌ای رو دانلود کنه، باید هزینه پرداخت کنه که گیت بهش دسترسی بده.
———
سیستم CRM شرکت توی این آدرس هست که میتونی مشاهده کنی و اگر سیستم لایسنس نیاز به ارتقا داشت انجام بدی:
/mnt/sda1/Projects/Webina/Webina/Plugins/Webino

درگاه‌های پرداخت:
دیجی پی - اعتباری - اقساطی:
https://www.mydigipay.com/developers/docs/upg/?php#4
زرین پال - نقدی:
https://www.zarinpal.com/docs/apiDocs/

۱. فونت پیشفرض پنل: Yekan Bakh Pro → `frontend/public/fonts/yekan-bakh/` (منبع: `/mnt/Mine/Software/Fonts/Yekan Bakh (Pro) @MafiaFont`)
۲. ماژول حسابداری به صورت کامل کپی بشه: /mnt/sda1/Projects/Webina/Webina/Plugins/Webino
۳. از سیستم لایسنس این پروژه استفاده بشه: /mnt/sda1/Projects/Webina/Webina/Plugins/Webino